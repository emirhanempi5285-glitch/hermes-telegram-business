manifest_version: 1
# Legacy-stable runtime identity: changing this would break existing Hermes
# install directories, config keys, and update/remove commands.
name: telegram-business-voice-transcriber
version: 0.5.0
description: "Hermes Telegram Business integration; currently provides voice and video-note transcription, conservative transcript cleanup, and Business-scoped replies."
author: neoromantic
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
